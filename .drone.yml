kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

clone:
  depth: 5

steps:
  - name: greeting
    commands:
      - echo hello world
  - name: change dir
    commands:
      - cd /opt/apps/teman-data-landing-page
      - ls -la
  - name: where IM
    commands:
      - pwd && ls -la
